# PostgreSQL + pgvector Requirements
# Install these packages for the semantic search project

# Database connectivity
psycopg2-binary>=2.9.0     # PostgreSQL adapter for Python
sqlalchemy>=2.0.0          # SQL toolkit and ORM (optional, for advanced use)

# Web framework
fastapi>=0.104.0           # Modern Python web framework
uvicorn[standard]>=0.24.0  # ASGI server for FastAPI
pydantic>=2.4.0            # Data validation using Python type annotations

# Embedding and ML
sentence-transformers>=2.2.0  # Pre-trained sentence embeddings
numpy>=1.26.0                 # Numerical computing
scikit-learn>=1.3.0          # Machine learning library (fallback embeddings)

# Utilities
python-multipart>=0.0.6   # Form data parsing
python-dotenv>=1.0.0      # Environment variable management

# Optional: For production
gunicorn>=21.0.0          # Production WSGI server
redis>=5.0.0              # Caching (if needed)

# Optional: For monitoring
prometheus-client>=0.18.0  # Metrics collection
structlog>=23.0.0         # Structured logging

# Development dependencies (optional)
pytest>=7.4.0             # Testing framework
pytest-asyncio>=0.21.0    # Async testing support
httpx>=0.25.0             # HTTP client for testing APIs
